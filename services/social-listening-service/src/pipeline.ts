import type { Input, Output } from "./schema.js";

/** Placeholder pipeline — Feature implementation TBD. */
export async function runPipeline(_input: Input): Promise<Output> {
  return {
    status: "not_implemented",
    message: "Social listening pipeline is scaffolded but not implemented yet",
  };
}
